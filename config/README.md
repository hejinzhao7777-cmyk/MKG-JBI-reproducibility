# Configuration

`MKG_JBI_SUBMISSION_LOCK_manifest.json` is the provenance manifest generated for the locked JBI analysis. It records the six cancers, principal external cohorts, hyperparameters, completed pipeline steps, output paths, and SHA-256 hashes.

